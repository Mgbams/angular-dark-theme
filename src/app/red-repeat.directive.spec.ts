/*import { RedRepeatDirective } from './red-repeat.directive';

describe('RedRepeatDirective', () => {
  it('should create an instance', () => {
    const directive = new RedRepeatDirective();
    expect(directive).toBeTruthy();
  });
});*/
